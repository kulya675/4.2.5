const searchBar = document.querySelector(".search-input");
const suggestList = document.querySelector(".suggest-list");
const suggestItems = suggestList.children;
const repList = document.querySelector(".rep-list");
const closeButtons = [];

export { searchBar, suggestList, suggestItems, repList, closeButtons };
